// Static version string. Replaces AXe's VersionPlugin, which ran `git describe` at build time.
// No build-time code execution; bumped by hand when a build is pinned (PINS.md).
let VERSION = "0.2.3"
